#pragma once
#include "MineGrid.h"

// Receives a grid from the recognizer and works out which squares would be good to click.
class Solver
{
public:
	static void Solve(MineGrid& grid) {
		for (int x = 1; x < 60; x++) {
			for (int y = 1; y < 60; y++) {
				
			}
		}
	}
};

